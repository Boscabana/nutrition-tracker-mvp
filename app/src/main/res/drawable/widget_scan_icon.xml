<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Scan Corners -->
    <item android:drawable="@drawable/ic_scan_corners" />

    <!-- App Icon inside (with padding so it fits in the corners) -->
    <item
        android:drawable="@drawable/app_icon_main"
        android:bottom="6dp"
        android:left="6dp"
        android:right="6dp"
        android:top="6dp" />
</layer-list>
